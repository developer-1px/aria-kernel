# Canonical move notice — `@p/devtools` → `@interactive-os/devtools`

`@p/devtools` 의 REC 모듈 정본은 `@interactive-os/devtools/rec` 로 이전 (2026-05-15).

| 원본 (여기) | 정본 |
|---|---|
| `src/rec/*` (`ReproRecorderOverlay`, `createReproRecorder`, ...) | `@interactive-os/devtools/rec` |

새 코드는 `@interactive-os/devtools/rec` 를 import. 이 패키지는 호환을 위해 일시 유지.

전체 인벤토리: `@interactive-os/devtools/INVENTORY.md`.
